# src/main/resources/application.dev.yml
spring:
  application:
    name: pokedex-bff
  datasource:
    url: jdbc:postgresql://localhost:5432/pokedex_db
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

app:
  csv:
    location: ./docker/data/csvs/ # <--- MUDANÇA CRÍTICA AQUI!

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder: TRACE
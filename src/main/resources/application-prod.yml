spring:
  datasource:
    url: jdbc:postgresql://db:5432/pokedex
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
  jackson:
    serialization:
      indent_output: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
